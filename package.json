{
  "name": "website",
  "private": true,
  "description": "Personal site — feruzurazaliev.com. Dev deps power the design-rules CI only; the site itself is static HTML/JSX with no build step.",
  "scripts": {
    "lint:css": "stylelint '**/*.css'",
    "lint:design": "node .github/scripts/check-design.mjs",
    "test:responsive": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "stylelint": "^16.10.0",
    "stylelint-config-recommended": "^14.0.1"
  }
}
